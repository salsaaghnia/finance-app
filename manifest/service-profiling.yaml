apiVersion: v1
kind: Service
metadata:
  name: profiling-app
  labels:
    app: profiling-app
spec:
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  selector:
    app: profiling-app